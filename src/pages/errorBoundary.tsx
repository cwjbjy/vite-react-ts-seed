// import { useRouteError } from 'react-router-dom';

const ErrorBoundary = () => {
  //   const error = useRouteError();
  //错误信息，可用来错误上报
  //   console.log(error);
  return <>错误页面</>;
};

export default ErrorBoundary;
