import MindGraph from './components/mindGraph';

const Graph = () => {
  return (
    <div style={{ padding: 20 }}>
      <MindGraph width="100%" height="calc(100vh - 60px)" />
    </div>
  );
};

export default Graph;
